'use client';

import PageFormDrawer from '@components/PageFormDrawer';
import TrafficRoutersCreateForm from '../TrafficRoutersCreateForm';

function TrafficRoutersDrawer() {
  return (
    <PageFormDrawer>
      <TrafficRoutersCreateForm />
    </PageFormDrawer>
  );
}

export default TrafficRoutersDrawer;
